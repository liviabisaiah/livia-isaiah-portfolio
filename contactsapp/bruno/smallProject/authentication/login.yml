info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/users/login
  body:
    type: json
    data: |-
      {
        "username": "cheese",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
